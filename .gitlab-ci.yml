image: node:14.15.5-alpine3.12

stages:
  - test

tests_stage:
  stage: test
  script:
    - apt update
    - apt install libnss3 libatk-b -y
    - npm install 
    - npm test    
  artifacts:
    when: always
    reports:
      junit:        
        - junit.xml
