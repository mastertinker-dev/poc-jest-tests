image: node:14.15.5-buster-slim

stages:
  - test

tests_stage:
  stage: test
  script:
    - apt update
    - apt install -y libgbm1
    - npm install 
    - npm test    
  artifacts:
    when: always
    reports:
      junit:        
        - junit.xml
